package com.gracearena.resort.user;

/**
 * GUEST books and manages their own stays. ADMIN additionally sees every booking
 * and enquiry through the reservations desk endpoints.
 */
public enum Role {
	GUEST,
	ADMIN
}
