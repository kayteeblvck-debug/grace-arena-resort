# Leave unset in development — Vite proxies /api to http://localhost:8080.
# Set this to the deployed backend URL for staging/production builds.
# VITE_API_BASE_URL=https://api.gracearenaresort.com/api
